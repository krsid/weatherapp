<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Test</title>
   <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <div class="content">
      <h3>Today's weather!☀️</h3>
        <form action="/" method="post">
          <input class="box" name="city" type="text" class="ghost-input" placeholder="Enter a City" required>
          <input class="btn" type="submit" class="ghost-button" value="Get Weather">
        </form>
        <% if(weather !== null){ %>
          <p><%= weather %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
    </div>
  </body>
</html>